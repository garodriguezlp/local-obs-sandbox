# Ensure shell scripts always use LF line endings
*.sh text eol=lf

# Ensure batch scripts always use CRLF line endings
*.bat text eol=crlf

# Ensure Python scripts always use LF line endings
*.py text eol=lf

# Ensure YAML files always use LF line endings
*.yaml text eol=lf
*.yml text eol=lf

# Ensure Markdown files always use LF line endings
*.md text eol=lf
